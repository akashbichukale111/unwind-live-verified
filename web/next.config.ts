import type { NextConfig } from "next";

// Skeleton only. Task 1 builds no UI; this exists so the surface the operator
// will eventually use has a home and a pinned Next.js version.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;

# Social post — #AllThingsAgenticHackathon

Post AFTER the video is public and the URL is known. Replace both
`⟨FILL⟩` placeholders before posting; do not post with placeholders live.

---

## Primary post (≤280 characters)

```
When a fact turns out false, every decision built on it should raise its hand. UNWIND does — 2,594 dependents culled to 78, zero model calls. Built for #AllThingsAgenticHackathon. Demo: ⟨FILL — video URL⟩ Code: ⟨FILL — repo URL⟩
```

Character count, verified with `python3 -c "print(len(open('/dev/stdin').read().strip()))"`:
fixed text (everything except the two URLs) is **193 characters**, leaving
**87 characters combined** for both real URLs once pasted in — comfortably
enough for a YouTube watch link (~43 chars) and a GitHub repo link (~40
chars) at 280 total, but **re-count on the actual platform after pasting the
real URLs**, since link-shortening behaviour varies by platform.

---

## Thread variant (if the platform supports a thread)

**1/4**
```
When a fact turns out false, nothing in most systems points backwards from the fact to the decisions built on it. So correction happens socially — someone remembers, sends an email, hopes. UNWIND records the edge instead. #AllThingsAgenticHackathon
```

**2/4**
```
A supplier lead time moves from 11 to 20 days. The reverse index finds 2,594 dependent decisions. An arithmetic cull — zero model calls — reduces that to the 78 that actually need a human. Enforced in CI: the build fails if a single model call reaches this path.
```

**3/4**
```
Four cards: WARRANT (a decaying, capability-scoped authority ledger — cold-start agents refuse by construction), UNWIND CORE (the cull), CONTROL TOWER (registry + gateway + durable runtime), COUNTERSIGN (Gemma independently verifies before any warrant mints).
```

**4/4**
```
Built it, audited it, found real gaps (three of four cards didn't exist partway through — an internal review caught it). Fixed them. 369 tests passing. Full evidence trail in the repo, not just claims.
Demo: ⟨FILL⟩
Code: ⟨FILL⟩
```

---

## Before posting

- [ ] Replace both `⟨FILL⟩` placeholders with the real, live video and repo URLs.
- [ ] Re-count the primary post's characters WITH the real URLs pasted in — do this on the platform itself, since link-shortening behaviour varies.
- [ ] Confirm `#AllThingsAgenticHackathon` is spelled and cased exactly as the hackathon's own materials use it before posting (case does not affect hashtag matching on most platforms, but verify once, not per-post).
- [ ] Post from an account that can be attributed to the entrant, per the hackathon's bonus-eligibility rule.
- [ ] Paste the live post URL into `submission/devpost.md` and `submission/CHECKLIST.md` once posted.

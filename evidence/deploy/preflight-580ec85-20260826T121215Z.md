# Deploy preflight + local browser verification at commit 580ec85

Run: 2026-08-26T12:12:15Z

## deploy preflight (no credentials required)
```
======================================================================
UNWIND — deploy preflight (no credentials required)
======================================================================
  PASS  deploy.sh parses
  PASS  Cloud Run region is a real region  (us-central1)
  PASS  Vertex location matches the verified live run  (global)
  PASS  deploy.sh separates region from Vertex location
  PASS  deploys the product surface  (gcloud run deploy --source .)
  PASS  Procfile binds 0.0.0.0 and honours $PORT
  PASS  API serves the UI from one origin  (3 static files + FastAPI)
  PASS  the dead Next.js skeleton is excluded from the build
  PASS  no foreign package manager reaches the build  (pip reads pyproject.toml)
  PASS  the buildpack's Python is pinned  (.python-version 3.13 ⊆ >=3.12)
  PASS  refuses to guess a project
  PASS  runtime roles listed  (roles/aiplatform.user, roles/datastore.user, roles/pubsub.publisher)
  PASS  no over-broad roles (no Owner/Editor)
  PASS  APIs enabled  (5 services)
  PASS  Pub/Sub topics match lib/config.py  (6 topics)
  PASS  Firestore rules are not deployed by gcloud
  PASS  Firestore indexes are not deployed by gcloud
  PASS  indexes.json parses  (12 composite indexes)
  PASS  container build  (no Dockerfile — buildpacks + Procfile, as designed)
  PASS  the app imports with no credentials


Preflight passed. This checks INPUTS, not the deploy itself.
Deploy with ./infra/deploy.sh, then prove it computes:
    make deploy-verify URL=https://...
```

## Local browser verification at this commit

Real Chromium (Playwright) against `services.api.main:app` running the same
way Cloud Run runs it (the root `Procfile` command), on the Firestore
emulator with `UNWIND_VERTEX_DISABLED=1`.

**This is a LOCAL verification. It is NOT a verification of the deployed
URL** — see the deployment blocker below.

| Suite | Result |
| --- | --- |
| `evidence/browser/verify_timemachine_and_media.py` | **43/43 passed** — Time Machine (4 missions, 13 checkpoints, arc renders, checkpoint detail opens), Media Lab (Veo/Lyria honestly `NOT_CONFIGURED`, no fake media), Consequence Preview, seven-card regression, ESC navigation |
| `evidence/browser/verify_all_cards.py` | **26/26 passed** — all seven cards render real content, mission runs end to end (13 stages), anonymous mutation refused, zero server-side errors |
| `evidence/browser/verify_evolution_panel.py` | passed — 7 criteria rows, serving version named, real mission scored 0.9500, no horizontal body scroll |

The evolution panel opens **synchronously in 1.8 ms**, before any of its three
reads return — the "open first, populate after" property, i.e. no dead button.

### Console errors

One class only, on every suite: `fonts.googleapis.com` is blocked by this
sandbox's egress proxy (`ERR_CERT_AUTHORITY_INVALID` /
`ERR_CONNECTION_RESET`). `verify_all_cards.py` classifies it itself —
*"no failed request is a product defect — only the deliberate 401 and the
sandbox-blocked font host"*. The stylesheet declares real fallback stacks, so
the page renders correctly without it.

### One harness note, not a defect

`verify_timemachine_and_media.py` fills the operator token `demo-tok`. A
server started with any other token in `UNWIND_OPERATOR_TOKENS` returns 401
on the protected mission-history read and the Time Machine correctly shows
zero missions. That is the panel behaving as designed, not a regression;
the run above uses `demo-tok`.

## DEPLOYMENT WAS NOT PERFORMED IN THIS SESSION

No Cloud Run revision was created, and none is claimed. Two independent
blockers, both properties of this remote sandbox rather than of the
repository:

1. **No `gcloud`, and no credentials of any kind.** `gcloud` is not installed
   and is not present anywhere on disk; there is no
   `~/.config/gcloud/application_default_credentials.json`, no
   `GOOGLE_APPLICATION_CREDENTIALS`, no service-account key, and no
   `UNWIND_PROJECT_ID`. Installing the SDK would not help: authentication
   would still require an interactive `gcloud auth login` or a key file,
   neither of which exists here.

2. **Egress policy denies `*.a.run.app`.** The session's HTTPS proxy answers
   `403 Forbidden` to `CONNECT unwind-hgeodtazqq-uc.a.run.app:443`
   (recorded by the proxy itself as `connect_rejected` — "gateway answered
   403 to CONNECT (policy denial)"). So the existing live URL cannot be
   reached from this session at all: not to deploy to, and not even to read
   the currently-serving revision. `/root/.ccr/README.md` states that
   organization policy denials must be reported rather than retried.

The deployed URL therefore still serves whatever revision was live before
this session. Every deployment claim elsewhere in this repository is from an
earlier pass and carries its own date; none was re-verified here, and none
was changed.

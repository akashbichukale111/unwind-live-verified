"""WARRANT (Card 0): a deterministic, decaying, capability-scoped quantity of
authority, minted only from countersigned human-validated outcomes.

See `warrant/DESIGN.md` for the invention, the prior-art position, and the
threat model. Nothing in this package makes a model call -- see
`tests/test_warrant_zero_model.py`.
"""

from __future__ import annotations
